# triple prime number

## / Overview

p,qの近さを利用した解読

## / Writeup

素数 $`p`$ に対して$`q=3p+d`$が素数になるような最小の$`d`$が設定されています。$`p>>d`$なので$`N = pq`$ は $`3p^2`$よりほんの少し大きいだけです。

そこで$`\lfloor N/3 \rfloor`$よりほんの少し大きくて素数になる数を$`p`$とすると解読できます。